<%= form_for(@group) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br>

  <ul>
    <%= f.collection_check_boxes :user_ids, @users, :id, :name do |b| %>
      <li>
        <%= b.label %>
        <%= b.check_box %>
      </li>
    <% end %>
  </ul>
  <%= f.submit %>
<% end %>